# LoL × 本地音乐 × 语音 Agent — 项目工作报告

**报告日期：** 2026-09-14  
**项目阶段：** 第一阶段（项目真实性审计）完成  
**项目根目录：** `C:\Users\1\Doubao\chats\2026-09-14\new-chat\lol-music-agent`

---

## 一、项目背景

### 1.1 项目定义

这是一个 **"英雄联盟信息助手 + 本地音乐助手"** 桌面软件，语音是主要的快捷操作方式，而非聊天机器人。

**核心目标：**
- 管理本地音乐
- 读取和整理 LoL 当前游戏状态
- 记住游戏时间和关键事件
- 在真正值得提醒的时候说话
- 在客户端阶段处理重复操作
- 提供玩家、英雄、战绩等辅助信息
- 减少游戏过程中需要分心操作的事情

### 1.2 产品原则

- **不为了 AI 而 AI**：能用确定性代码解决的问题优先用代码
- **AI 只解决"人话 → 意图"**：LLM 不直接控制电脑
- **信息可以丰富，语音必须克制**：监控详细，播报稀疏
- **Read > Infer**：游戏已提供的信息不自己计算

### 1.3 明确禁止

❌ 自动控制英雄移动/释放技能/攻击/走位  
❌ 游戏内视觉识别 / OBS 识别 / 游戏内存读取 / DLL 注入  
❌ 修改游戏文件 / 模拟鼠标键盘操作实现游戏玩法  
❌ 游戏机器人 / 聊天机器人式人格陪聊

---

## 二、环境搭建工作

### 2.1 已完成的环境配置

| 项目 | 状态 | 详情 |
|------|------|------|
| Python 3.11.9 | ✅ 已安装 | `C:\Program Files\Python311\python.exe` |
| Git 2.55.0 | ✅ 已安装 | `C:\Program Files\Git\bin\git.exe` |
| 核心依赖包 | ✅ 已安装 | requests, pyyaml, pydantic, mutagen, faster-whisper, openai, pyaudio, edge-tts 等 |
| mpv 播放器 | ✅ 已确认 | `D:\新建文件夹 (2)\mpv.exe` v0.41.0 |
| 音乐文件 | ✅ 已确认 | `D:\落雪\音乐文件`，144 首 FLAC |
| 本地模型 | ✅ 已确认 | Qwen3-ASR 0.6B/1.7B、Qwen3-4B/8B、Jinx-4B、Qwen3.5-4B |
| LoL 客户端 | ✅ 已确认 | `D:\WeGameApps\英雄联盟（含经典模式）` |

### 2.2 遇到的问题

| 问题 | 状态 | 说明 |
|------|------|------|
| 无虚拟环境 | ⚠️ 未解决 | 依赖通过 `--user` 全局安装，需手动设置 `PYTHONPATH` |
| llama-cpp-python 编译失败 | ❌ 未解决 | 系统无 C++ 编译器（CMake Error） |
| LM Studio 未正确安装 | ❌ 未解决 | 仅存在更新器，主程序缺失，需人工安装 |
| Windows 长路径未启用 | ❌ 未解决 | 注册表 HKLM 权限拒绝 |

---

## 三、代码实现工作

### 3.1 项目结构总览

**总计：40 个 Python 文件**

```
lol-music-agent/
├── main.py                 # 主入口（195行）
├── 启动.bat                # Windows 启动脚本
├── README.md               # 项目说明
├── requirements.txt        # 依赖清单
├── config/
│   └── settings.yaml        # 全局配置
├── app/
│   ├── core/               # 核心层（7文件）
│   │   ├── config.py       # 配置加载
│   │   ├── event_bus.py    # 事件总线
│   │   ├── command_bus.py  # 命令总线
│   │   ├── state_store.py  # 状态存储
│   │   ├── scheduler.py    # 调度器
│   │   ├── logger.py       # 日志
│   │   └── __init__.py
│   ├── music/              # 音乐模块（7文件）
│   │   ├── database.py     # SQLite 数据库
│   │   ├── scanner.py      # 音乐扫描
│   │   ├── mpv.py          # mpv 控制
│   │   ├── queue.py        # 播放队列
│   │   ├── search.py       # 音乐搜索
│   │   ├── controller.py   # 音乐控制器
│   │   └── __init__.py
│   ├── lol/                # LoL 模块（8文件）
│   │   ├── events.py        # 事件类型定义
│   │   ├── live_client.py   # 2999 API 客户端
│   │   ├── snapshot.py      # 游戏快照
│   │   ├── state_diff.py     # 状态差异检测
│   │   ├── game_alarm.py    # 游戏提醒引擎
│   │   ├── lcu.py           # LCU 客户端连接
│   │   ├── runtime.py        # LoL 运行时
│   │   └── __init__.py
│   ├── voice/              # 语音输入（4文件）
│   │   ├── asr.py           # 语音识别
│   │   ├── intent.py        # 意图识别
│   │   ├── runtime.py       # 语音运行时
│   │   └── __init__.py
│   ├── speech/             # 语音输出（4文件）
│   │   ├── tts.py           # 语音合成
│   │   ├── speech_judge.py  # 播报评判
│   │   ├── kill_aggregator.py # 击杀合并
│   │   └── __init__.py
│   ├── player/             # 玩家分析（空壳）
│   │   └── __init__.py
│   └── ui/                 # UI 控制台（空壳）
│       └── __init__.py
├── data/
│   └── test_music.db        # 测试音乐库（144首）
└── logs/                   # 日志目录（空）
```

### 3.2 各模块实现状态

| 模块 | 文件数 | 实现状态 | 说明 |
|------|--------|----------|------|
| `app/core/` | 7 | ✅ 已实现 | 事件总线、命令总线、状态存储、调度器、日志、配置 |
| `app/music/` | 7 | ✅ 已实现 | 数据库、扫描、mpv 控制、队列、搜索、控制器 |
| `app/lol/` | 8 | ✅ 已实现 | 事件、2999 客户端、快照、状态差异、游戏提醒、LCU、运行时 |
| `app/voice/` | 4 | ✅ 已实现 | ASR、意图识别、运行时 |
| `app/speech/` | 4 | ✅ 已实现 | TTS、播报评判、击杀合并 |
| `app/player/` | 1 | ❌ 空壳 | 仅 `__init__.py`，无实际实现 |
| `app/ui/` | 1 | ❌ 空壳 | 仅 `__init__.py`，无实际实现 |

---

## 四、测试验证工作

### 4.1 音乐库测试

**测试对象：** `data/test_music.db`

| 验证项 | 结果 |
|--------|------|
| 数据库可打开 | ✅ 是 |
| music 表存在 | ✅ 是（144 行） |
| music_fts 表存在 | ✅ 是（144 行） |
| music_highlight 表存在 | ✅ 是（0 行，预留） |
| 随机抽查 5 首文件存在 | ✅ 全部存在 |
| 中文搜索"勇敢" | ⚠️ 0 结果（可能库中无此关键词） |

### 4.2 mpv 测试

| 验证项 | 结果 |
|--------|------|
| mpv.exe 启动 | ✅ 成功 |
| Named Pipe 创建 | ✅ 成功 |
| Python 连接管道 | ✅ 成功 |
| JSON IPC 命令收发 | ✅ 成功（返回 mpv 版本） |
| mpv.py 封装 `_wait_for_pipe` | ❌ 有 bug（open() 阻塞） |
| loadfile / pause / stop 封装测试 | ⚠️ 未通过（因管道连接 bug 卡住） |

### 4.3 LCU 腾讯服测试

**重大发现：腾讯服 LCU 完全可用！**

| 验证项 | 结果 |
|--------|------|
| 标准 lockfile | ❌ 不存在（腾讯服不使用） |
| 连接信息获取方式 | ✅ 从 `LeagueClientUx.log` 命令行参数提取 |
| 端口 58334 | ✅ 可连接 |
| Token 认证 | ✅ 有效 |
| `/lol-gameflow/v1/gameflow-phase` | ✅ 200，返回 `"ChampSelect"` |
| `/lol-summoner/v1/current-summoner` | ✅ 200，等级 38 |
| `/lol-champ-select/v1/session` | ✅ 200 |
| `/lol-matchmaking/v1/search` | ✅ 200 |
| `/help` | ⚠️ 仅返回 3 个端点 |
| `lcu.py` 封装连接 | ❌ 未通过（锁文件查找失败） |

### 4.4 核心架构测试

#### EventBus
- ✅ 发布/订阅机制正常工作
- ✅ 支持多事件类型订阅

#### StateDiff
- ✅ 构造两份 Snapshot 可产生 7 个事件
- 事件类型：player.level_up, game.champion_kill, game.dragon_killed, game.tower_destroyed, player.item_changed, player.summoner_spell_used, game.time_update
- ❌ **事件类型字符串与 EventBus 的 EventType 不一致**（`player.level_up` vs `game.player_level_up`），导致 3/7 事件无法被正确订阅

#### GameAlarm
- ❌ **初始化失败**：引用了不存在的 `EventType.GAME_START`（应为 `GAME_STARTED`）
- 创建 Alarm 的逻辑代码存在，但未通过完整测试

#### Intent Rules
- ✅ 13/13 测试用例全部通过
- 覆盖：下一首、暂停、继续播放、上一首、音量+、音量-、两分钟提醒、闪现、技能、眼位、搜索、跳高潮

### 4.5 ASR / TTS 测试

| 验证项 | 结果 |
|--------|------|
| faster-whisper 已安装 | ✅ |
| PyAudio 已安装 | ✅（20 个输入设备） |
| Qwen3-ASR 模型文件存在 | ✅ |
| llama-cpp-python | ❌ 未安装（无 C++ 编译器） |
| 实际音频识别测试 | ❌ 未进行 |
| Edge TTS 已安装 | ✅ |
| Edge TTS 实际生成 | ✅ 成功（33120 字节 MP3） |

### 4.6 2999 测试

- ❌ **未测试**：2999 端口未开放（LoL 未在游戏中）
- 需要人工进入 LoL 训练模式才能验证

---

## 五、发现的问题清单

### 5.1 阻断性问题（🔴）

| # | 问题 | 影响 |
|---|------|------|
| 1 | LCU 腾讯服连接方式不兼容 | `lcu.py` 锁文件查找在腾讯服必然失败 |
| 2 | StateDiff 与 EventBus 事件类型不一致 | 3/7 游戏事件丢失，核心数据链路断裂 |
| 3 | GameAlarm 引用不存在的事件类型 | GameAlarmEngine 初始化直接崩溃 |
| 4 | mpv 管道连接逻辑 bug | `_wait_for_pipe` 的 open() 阻塞，重试无效 |

### 5.2 重要问题（🟡）

| # | 问题 | 影响 |
|---|------|------|
| 5 | LM Studio 未正确安装 | Intent LLM 后端不可用 |
| 6 | llama-cpp-python 无法编译 | 本地模型直接加载方式不可用 |
| 7 | 无虚拟环境 | 依赖全局安装，项目可移植性差 |
| 8 | 正式数据库不存在 | 主程序运行会创建空库 |
| 9 | player/ 和 ui/ 完全空壳 | 核心功能未实现 |
| 10 | 2999 未实际测试 | 需要人工进入游戏验证 |

---

## 六、项目功能完成度评估

| 功能 | 计划定位 | 当前状态 | 完成度 |
|------|----------|----------|--------|
| 🎙️ 语音识别 ASR | 必须 | 依赖齐全，未实际音频测试 | 40% |
| 🧠 人话理解 / Intent | 必须 | 规则匹配完成，LLM 后端不可用 | 60% |
| 🎵 本地音乐基础控制 | 必须 | mpv 封装有 bug，底层可用 | 50% |
| 🎵 音乐模糊搜索 | 必须 | SQLite+FTS5 完成，中文效果待验证 | 70% |
| 🎵 跳到高潮 | 可选 | 数据库表已建，无实际分析 | 10% |
| 🎮 LCU 客户端连接 | 必须 | 底层 API 可用，封装需修复 | 50% |
| 🎮 自动接受 | 可选 | 未实现 | 0% |
| 🎮 自动 Ban / Pick | 可选 | 未实现，架构预留 | 0% |
| 👥 玩家身份获取 | 必须 | 未实现 | 0% |
| 👥 玩家历史战绩 | 必须 | 未实现 | 0% |
| 👥 位置 / 熟练英雄分析 | 可选 | 未实现 | 0% |
| ⏱️ 游戏计时与提醒 | 必须 | 初始化崩溃，需修复 | 30% |
| 🎮 2999 游戏状态 | 必须 | 代码存在，未实际测试 | 30% |
| 🖥️ UI 控制台 | 必须 | 空壳 | 0% |

**整体完成度：约 30-35%**

---

## 七、下一步工作建议

### 必须修复（阻断性）

1. **修复 LCU 腾讯服连接** — 改用进程命令行参数获取端口和 token
2. **统一事件类型定义** — 消除 GameEventType 与 EventType 的不一致
3. **修复 GameAlarm 事件引用** — `GAME_START` → `GAME_STARTED`
4. **修复 mpv 管道连接** — 先用 `os.path.exists()` 检查再 open

### 需要人工操作

5. **人工安装 LM Studio** — 加载 Qwen3-4B 模型，启动 API 服务器
6. **人工进入 LoL 训练模式** — 验证 2999 端口完整数据链路

### 建议改进

7. 创建虚拟环境隔离依赖
8. 初始化正式音乐数据库
9. 实现 player 模块（玩家分析）
10. 实现 UI 控制台

---

## 八、关键技术决策记录

| 决策 | 选择 | 原因 |
|------|------|------|
| ASR 主引擎 | faster-whisper | llama-cpp-python 编译失败，faster-whisper 已可用 |
| Intent 后端 | OpenAI 兼容 API（LM Studio） | llama-cpp-python 不可用，改用 LM Studio API |
| 音乐数据库 | SQLite + FTS5 | 简单可靠，第一版足够 |
| 音频后端 | mpv JSON IPC | 不抢焦点，不弹窗，不影响 LoL 音效 |
| 事件架构 | EventBus + CommandBus | 模块解耦，便于扩展 |
| TTS 第一阶段 | Edge TTS | 快速可用，第二阶段再考虑 Kokoro |

---

**报告生成时间：** 2026-09-14  
**项目状态：** 第一阶段审计完成，等待下一步指示
